<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="SCE2.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SCE2">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>
            <!-- Other app resources here -->
            <Style TargetType="ScrollViewer" x:Key="HoverScrollViewerStyle">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollViewer">
                            <Grid x:Name="RootGrid">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <!-- Top separator (border) -->
                                <Border Grid.Row="0"
                                Height="1"
                                Background="{TemplateBinding BorderBrush}" />
                                <ScrollContentPresenter x:Name="ScrollContentPresenter"
                                               Grid.Row="1"
                                               Content="{TemplateBinding Content}"
                                               ContentTemplate="{TemplateBinding ContentTemplate}"
                                               Margin="{TemplateBinding Padding}"/>
                                <ScrollBar x:Name="HorizontalScrollBar"
                                   Orientation="Horizontal"
                                   Grid.Row="2"
                                   Visibility="Collapsed"
                                   Maximum="{TemplateBinding ScrollableWidth}"
                                   Value="{TemplateBinding HorizontalOffset}"
                                   ViewportSize="{TemplateBinding ViewportWidth}"/>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="HorizontalScrollBar.Visibility" Value="Visible"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <Style TargetType="MenuFlyoutPresenter">
                <Setter Property="Background" Value="{StaticResource SystemChromeMediumColor}" />
                <Setter Property="CornerRadius" Value="0"/>
            </Style>

            <Style TargetType="ScrollBar">
                <Setter Property="Width" Value="15"/>
                <Setter Property="Visibility" Value="Visible"/>
            </Style>

            <SolidColorBrush x:Key="ChromeHighColor" Color="{ThemeResource SystemChromeHighColor}" />
        </ResourceDictionary>   
    </Application.Resources>
</Application>
